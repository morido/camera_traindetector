/*
  defines:
  MAXPACKETSIZE - The maximum size of data in bytes which fit into a single
  UDP-packet in IPv4; *Note:* Due to the simplicity of the C language
  this can only be a #define rather than a real constant
*/
#define MAXPACKETSIZE 548
